---
apiVersion: v1
kind: Service
metadata:
  name: fast-app-service
  namespace: default
spec:
  selector:
    app: fast-app
  type: LoadBalancer
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
