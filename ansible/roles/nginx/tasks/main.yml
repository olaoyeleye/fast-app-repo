- name: Update apt cache when needed
  apt:
    update_cache: yes
  when: ansible_pkg_mgr == 'apt'

- name: Install Nginx
  package:
    name: nginx
    state: present

- name: Ensure Nginx is enabled and running
  service:
    name: nginx
    state: started
    enabled: yes
